import React from "react";
import { DefaultTemplate, Posts } from "../components";
const Home = () => {
  return (
    <DefaultTemplate>
      <Posts />
    </DefaultTemplate>
  );
};

export default Home;
